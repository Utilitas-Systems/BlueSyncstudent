# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tauri updater — never commit minisign private keys
bluesync-signing.key
bluesync-signing.key.backup-*
bluesync-signing.key.pub.backup-*
*-signing.key
*-signing.key.old
*.minisign.key

# Windows code-signing PFX / merge configs (never commit)
*.pfx
src-tauri/tauri.bundle.signing.json

# Tauri
src-tauri/target
src-tauri/Cargo.lock
src-tauri/icons/icon-source-1024.png

# Environment
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
